set(COMPONENT_ADD_INCLUDEDIRS ".")

# Edit following two lines to set component requirements (see docs)
set(COMPONENT_REQUIRES "spidriver")
set(COMPONENT_PRIV_REQUIRES )

set(COMPONENT_SRCDIRS ".")

register_component()
